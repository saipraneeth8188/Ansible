name: matrix fail fast tutorial

on:
    workflow_dispatch:

jobs:
    Build:
        name: Build-Job
        strategy:
            fail-fast: false
            matrix:
                os: [ubuntu-latest,ubuntu-sai-version,windows-latest]
        runs-on:
            ${{ matrix.os}}
        steps:
            - name: running command
              run: echo "running in ${{ matrix.os }}"